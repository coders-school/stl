#pragma once
#include <list>
#include <deque>
#include <string>
#include <map>


std::map<int, std::string> removeDuplicateAndTranformToMap(const std::list<std::string>& list, const std::deque<int>& deque)
{
    std::map<int, std::string> p{{1, "ddd"}};

return p; 

}
