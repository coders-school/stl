#include "palindroms.hpp"

bool is_palindrome(std::string word)
{
    return true;
}
bool isOutOfRange(const char c,const int upperBound1,const int upperBound2)
{
    const int index = (const int ) c;
    if(index<upperBound1||index>upperBound2)
        return true;


}