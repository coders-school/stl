#include "isPalindrome.hpp"

#include <algorithm>

bool is_palindrome(const std::string& text) {
    std::string pure_text;
    std::copy_if(text.begin(), text.end(), pure_text.begin(), 
    [](char letter)
    {
        
    });
    
}
