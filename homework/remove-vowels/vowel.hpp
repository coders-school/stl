#pragma once

#include <algorithm>
#include <string>
#include <vector>
#include <array>

void removeVowels(std::vector<std::string> &vowel);
const std::array<char, 12> arr = {'A', 'E', 'I', 'O', 'U', 'Y',
                          'a', 'b', 'i', 'o', 'u', 'y'};