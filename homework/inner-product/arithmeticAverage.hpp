#pragma once

#include <vector>

double ArithmeticAverage(const std::vector<int>& rhs, const std::vector<int>& lhs);
double Distance(const std::vector<int>& first, const std::vector<int>& second);