#pragma once
#include <cmath>
#include <functional>
#include <numeric>
#include <vector>

double ArithmeticAverage(std::vector<int>, std::vector<int>);
double Distance(std::vector<int>, std::vector<int>);