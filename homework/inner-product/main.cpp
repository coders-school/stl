#include <iostream>
#include "arithmeticAverage.hpp"

int main() {
    std::vector<int> first = {6,2,8};
    std::vector<int> second = {-1, 5, 0};
    std::cout << Distance(first, second);
    return 0;
}