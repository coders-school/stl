#include <deque>
#include <forward_list>
#include <string>

bool LexCompare(const std::string&, const std::string&);
bool LengthCompare(const std::string&, const std::string&);

std::deque<std::string> lengthSort(std::forward_list<std::string>&);
