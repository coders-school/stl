#include <deque>
#include <forward_list>
#include <iostream>
#include <string>

bool sortBy(const std::string& j, const std::string& k);
std::deque<std::string> lengthSort(std::forward_list<std::string>& dTab);