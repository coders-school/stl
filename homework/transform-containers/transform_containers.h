#include <deque>
#include <string>
#include <iterator>
#include <list>
#include <map>
#include <algorithm>

std::map<int, std::string> removeDuplicateAndTranformToMap(std::list<std::string>& list, std::deque<int>& deque);
