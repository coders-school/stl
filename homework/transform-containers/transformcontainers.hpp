#pragma once
#include <iostream>
#include <deque>
#include <list>
#include <map>
#include <algorithm>

std::map<int, std::string> removeDuplicateAndTranformToMap(std::list<std::string>& str, std::deque<int>& deq);
