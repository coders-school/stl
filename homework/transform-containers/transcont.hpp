#pragma once

#include <iostream>
#include <list>
#include <deque>
#include <map>
#include <iterator>
#include <algorithm>

std::map<int, std::string> 
removeDuplicateAndTranformToMap(std::list<std::string> list, std::deque<int> deque);
