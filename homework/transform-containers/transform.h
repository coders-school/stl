#include <algorithm>
#include <deque>
#include <list>
#include <map>
#include <string>

std::map<int, std::string> removeDuplicateAndTranformToMap(std::list<std::string>&, std::deque<int>&);
void removeDuplicate(std::list<std::string>&, std::deque<int>&);
