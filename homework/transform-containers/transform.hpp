#include <algorithm>
#include <iostream>
#include <list>
#include <deque>
#include <string>
#include <map>
#include <vector>

std::map<int, std::string> removeDuplicateAndTranformToMap(std::list<std::string> lis, std::deque<int> deq);
