#pragma once
#include <iostream>
#include <list>
#include <map>
#include <string>
#include <deque>
#include <algorithm>

std::map<int, std::string> removeDuplicateAndTranformToMap(std::list<std::string> lst, std::deque<int> deq);
