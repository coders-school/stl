#include "sort.hpp"

#include <algorithm>
#include <forward_list>
#include <string>

std::deque<std::string> lengthSort(std::forward_list<std::string>& flist) {
    auto size = 0;
    for (auto el : flist) {
        if (el.size() == size) {
        }
    }
}