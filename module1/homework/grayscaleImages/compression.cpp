#include "compression.hpp"

#include <iostream>


void printMap(const Bitmap& bitmap){

   for(const auto row : bitmap){
      for(const auto pixel : line){
            std::cout<<it;
      }
      cout<<'\n';
   }
}
CompressedBitmap compressLine(cout std::array<uint8_t, width>& line){
   CompressedBitmap result;

}
 