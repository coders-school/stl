#include "compression.hpp"

std::array<std::array<int, width>, height> decompressGrayscale(const std::vector<std::pair<int, int>>& compressedData)
{
    std::array<std::array<int, width>, height> decompressedData;

    

    return decompressedData;
}